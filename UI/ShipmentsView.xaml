<UserControl x:Class="ONE.UI.ShipmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <Grid>
        <!-- DataGrid displaying the spreadsheet-like data -->
        <DataGrid x:Name="ShipmentsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="10"/>

        <!-- "Add Column" Button anchored at the top-right -->
        <Button x:Name="AddColumnButton"
                Content="➕ Column"
                Click="AddColumn_Click"
                Width="100"
                Height="30"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,5,5,0"/>

        <!-- "Add Row" Button anchored at the bottom-left -->
        <Button x:Name="AddRowButton"
                Content="➕ Row"
                Click="AddRow_Click"
                Width="100"
                Height="30"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="5,0,0,5"/>
    </Grid>
</UserControl>
